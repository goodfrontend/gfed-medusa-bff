extend schema
  @link(
    url: "https://specs.apollographql.com/federation/v2.3"
    import: [
      "@key"
      "@external"
      "@requires"
      "@provides"
      "@tag"
      "@shareable"
    ]
  )

scalar DateTime
scalar JSON

type Query {
  _empty: String
}
